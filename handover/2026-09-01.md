# Shift Handover — Day — 2026-09-01

**Window:** 2026-09-01 06:00 → 2026-09-01 19:39 (IST)

> Team handover (WhatsApp format) — written for readers without prior context on today's work.

---

*Day shift — 1 Sep*

*Done today:*
• Promo apps EKS migration cost sheet ✅ — counted 32 servers in pg2-nonprod, estimated ~$700/mo today vs ~$570/mo on EKS (for Jira)
• ATS/PDI frontend Jenkins pipeline ✅ — one shared job now builds ATS + PDI staging apps from inventory-platform repo
• Yesterday's carry-over items all closed ✅ (Incentive deploy, Promtail on new Amazon Linux boxes, CLM video access, TMS QA logs, Prometheus metric limit)

*Still open:*
• Prod pgvalidate *application* logs not reaching Loki reliably — access/tomcat logs are fine; app logs have gaps on busy servers

*What we learned today:*

*What helped:*
• Testing Loki directly from the server proved the network path is fine — not a firewall/connectivity issue
• Searching logs with the right labels (job name + server IP) finds errors; alert labels from Grafana don't work in Loki
• Promtail is running and sending logs — nothing is being dropped at the agent

*What misled us:*
• Looked like logs were "missing from Loki" but we were searching with wrong labels (from the alert, not from Loki)
• Promtail doesn't have a /targets page like some other agents — use metrics + positions file instead
• On Java 21 servers the app writes to *service*.log files, but promtail was watching *application*.log (old naming)
• Log rotation uses "copytruncate" — every ~4 min on busy boxes ~200MB of logs rotate to a new filename that promtail doesn't watch, so those lines never reach Loki

*For night shift:*
• Fix: make promtail watch rotated log files too, and change log rotation so it doesn't truncate in place
• After fix: confirm app logs flow continuously on a busy prod pgvalidate box
• Loki tenant for pgvalidate prod: ppsl-promotions-90d

*Night todo:*
1. Apply log shipping fix on prod pgvalidate servers
2. Check Loki shows app logs without gaps on a high-traffic box
