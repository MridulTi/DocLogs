# Shift Handover — Day — 2026-09-01

**Window:** 2026-09-01 06:00 → 2026-09-01 19:27 (IST)  
**Generated:** 2026-09-01T19:27:00+05:30

---

## TL;DR

Day shift focused on **prod pgvalidate Loki gaps** (wrong log path on j21, copytruncate rotation drops), **pg2-nonprod EKS migration cost estimate** (32 nodes, ~$700/mo EC2 vs ~$570/mo EKS), and **ATS/PDI monorepo Jenkins** for inventory-platform. Root cause for missing app logs: j21 writes `*service*.log` but promtail tailed `*application.log`; high-traffic prod also loses ~200MB every ~4 min from `copytruncate` + narrow glob. Carry forward from yesterday: TMS QA→Loki, Prom sample_limit (dev to cut metrics).

---

## Closed

### ansible — Prod pgvalidate-j21 Loki app logs
- Root cause: promtail `__path__` = `*application.log` but j21 log4j2 writes `*service*.log` (tomcat/access OK)
- Git fix: `tag_Service_ppsl-prod-promo-pgvalidate-j21.yml` + j21-canary → `*service*.log`
- Loki connectivity verified (204 in ~0.5s); intermittent `context deadline exceeded` = 20MB batch / 60s timeout
- Host `pawspromopromotionsspringboot102211`: manual `timeout: 120s` applied; promtail healthy (460k+ sent, 0 dropped)

### pg2-nonprod — EKS migration cost estimate (read-only)
- Inventoried **32 running nodes** (pgvalidate 11, promo-admin 7, admin-consumer 8, engine-admin 6) across dev1/dev2/qa/staging
- Current stack ~**$700–770/mo** (EC2 $410 + EBS $244 + ALB $49 + LCU)
- EKS target ~**$570–600/mo** (1 cluster, 8× m7g.large, 4 namespaces) — ~15–20% saving if consolidated
- Jira-ready summary prepared; Cost Explorer validation requested

### merchant-deploy-k8s — ATS/PDI monorepo Jenkins
- Created `Jenkinsfile-monorepo` for inventory-platform (ATS mfe1/mfe2 + PDI mfe)
- Created separate `Dockerfile-monorepo`; restored original `Dockerfile` for legacy jobs
- Maps: ATS → `ats-ops-web-app`, PDI → `pdi-ops-web-app`, S3 backup prefixes per target

### Tooling — shift-handover skill (from prior session, refined)
- Updated skill for WhatsApp-first one-pass handover + domain patterns (AL2023 promtail)

---

## Open

| Item | State | Next step |
|------|-------|-----------|
| **Prod pgvalidate app-log gaps (intermittent)** | Root cause found — `copytruncate` + glob `*application.log` drops ~200MB/~4min on high-traffic j21 | Widen glob to `*application.log*` or `*service*`; fix logrotate to `copy`+`create`; apply j21 path fix on all prod nodes |
| **timeout 120s on prod j21** | Manual on 102211 only | Commit `timeout: 120s` (+ optional `batchsize: 10485760`) to group_vars |
| **TMS QA logs → Loki** | Unresolved (from 31 Aug) | Debug TMS QA box promtail pipeline |
| **Prom sample_limit / TMS metrics** | Not fixing via infra (from 31 Aug) | Dev to reduce telegraf/TMS metric cardinality |
| **EKS cost estimate** | Draft done | Validate in Cost Explorer before budget sign-off |

---

## Context for next shift

### Prod pgvalidate Loki — two separate issues
1. **Wrong filename (j21):** app logs in `*service*.log`, promtail had `*application.log` — fixed in git, needs rollout
2. **Intermittent drops (all high-traffic prod j21):** logrotate `maxsize 200M` + `copytruncate` rotates to `.2026-*` files that **don't match** promtail glob → ~250MB lost per rotation (~every 4 min on busy nodes). Access/tomcat logs same mechanism but rotate less often so gaps less visible.

### Grafana / Loki query (affordability prod alert)
- Alert labels (`techteam`, `instance`) are Prometheus-only — **not in Loki**
- Use tenant **`ppsl-promotions-90d`** and labels: `job=ppsl-promo-pgvalidate-app-log|tomcat-log`, `host=<IP>`, `stack=prod`
- Example: `{job="ppsl-promo-pgvalidate-tomcat-log", host="10.103.103.167"} |= "promosearch/tnc" |= " 500 "`

### EKS migration snapshot (pg2-nonprod)
- 32 Graviton nodes, 3 shared internal ALBs, 2,677 GB EBS
- Recommended: 1 cluster, namespaces dev1/dev2/qa/staging, 8× m7g.large workers

### ATS monorepo Jenkins params
- `GIT_APP_BUILD_SERVER_NAME`: `ats-staging-mfe1` | `ats-staging-mfe2` | `pdi-staging-mfe`
- Repo: `ppslteam/inventory-platform`, Dockerfile: `mfe-app/docker/Dockerfile-monorepo`

---

## Next steps (priority)

1. Roll out j21 promtail path fix (`*service*.log`) + widen glob / fix logrotate on prod pgvalidate
2. Commit `timeout: 120s` to prod j21 group_vars (don't lose manual host fix on next promtail run)
3. TMS QA → Loki debug
4. EKS estimate: Cost Explorer validation for Jira ticket
5. ATS monorepo: create Jenkins job with parameter checklist (Phase 3 if not done)

---

## WhatsApp paste

*Day shift — 1 Sep*

*Closed:*
• Prod pgvalidate-j21 Loki — root cause wrong log path (*service*.log not *application.log) ✅ git fixed
• pg2-nonprod EKS cost estimate ✅ (32 nodes, ~$700 EC2 vs ~$570 EKS/mo)
• ATS/PDI monorepo Jenkins + Dockerfile-monorepo ✅

*Open:*
• Prod pgvalidate app-log intermittent drops — copytruncate + narrow promtail glob (~200MB lost every ~4min on busy nodes)
• timeout 120s manual on 102211 — commit to group_vars
• TMS QA → Loki (carry from 31 Aug)
• Prom sample_limit — dev to cut metrics (carry from 31 Aug)

*Remember:*
• Loki queries: use job/host labels + tenant ppsl-promotions-90d — NOT alert labels techteam/instance
• Fix drops: widen glob *application.log* OR fix logrotate copy+create
• j21 path: *service*.log — re-run --tags promtail on prod nodes

*Night priority:*
1. Roll out pgvalidate promtail/logrotate fix on prod
2. TMS QA → Loki
3. EKS Cost Explorer validation for Jira

---

## Sources

- Chats: `a288f9e3` (prod pgvalidate Loki/j21), `da4dd4a1` (EKS cost, affordability Loki drops), `60bec1e4` (ATS monorepo Jenkins)
- Prior handover: `2026-08-31-day-handover-whatsapp.txt` (TMS QA, Prom sample_limit carry-forward)
