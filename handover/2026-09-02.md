# Shift Handover — Day — 2026-09-02

**Window:** 2026-09-02 06:00 → 2026-09-02 16:48 (IST)

> Team handover (WhatsApp format) — written for readers without prior context.

---

*Day shift — 2 Sep*


*Done today:*

• FSE platform Grafana dashboard (TMD-23324) ✅ — OpenSearch + ALB + Kafka panels working (v11)

• OpenSearch health/shards/CPU via cloudwatch-kyb (managed domain fsm)

• ALB request count, 4xx/5xx, latency — fixed and verified after v11 re-import

• Kafka topic partition count table added (per topic, respects cluster dropdown)

• ALB/NGINX log-metric section + Kafka CPU/memory/disk graphs


*Still open:*

• Prod pgvalidate application logs → Loki — log shipping fix not rolled to prod yet (carry from yesterday)

• Open Jiras: TMD-23322 (middleware dashboard), TMD-24533 (vault secrets), TMD-24571 (TMS CNS secrets), TMD-24536 (promo docker QA/staging)


*Waiting approval (In Review):*

• TMD-23324 — platform dashboard

• TMD-24576 — FSM NGINX whitelist

• TMD-24475 — inventory-platform Jenkins monorepo


*What we learned today:*


*What helped:*

• Testing CloudWatch from CLI — showed common-appalb has full metrics; external nginx ALB only has request count

• OpenSearch CloudWatch needs ClientId (761904723706) + domain name fsm — not domain alone

• ALB fix pattern: hidden query with partial LB match, then SUM(A) metric math in Grafana


*What misled us:*

• Metric Math SEARCH with $alb inside expression — Grafana does not substitute variables there → all panels showed no data

• Picking fse-nginx-prod-alb-external for 5xx/latency panels — that ALB simply does not publish those metrics in AWS

• Trying to aggregate AZs with SEARCH in v8 — broke OpenSearch and ALB together; needed separate fixes per section


*For night shift:*

• Priority: roll out pgvalidate log shipping fix on prod (wider promtail glob + logrotate change)

• Verify app logs in Loki without gaps on a busy prod box (tenant ppsl-promotions-90d)

• Dashboard JSON is in ppsl-tools repo — v11 is the good import version


*Night todo:*

1. Apply pgvalidate app-log fix on prod servers

2. Confirm continuous Loki flow on high-traffic prod box

3. Open Jiras if bandwidth (vault / TMS secrets)
