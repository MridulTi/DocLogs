# Shift Handover — Day — 2026-09-01

**Window:** 2026-09-01 06:00 → 2026-09-01 19:31 (IST)  
**Generated:** 2026-09-01T19:31:00+05:30  
**Updated:** user-confirmed status

---

## TL;DR

Day shift: **prod pgvalidate Loki investigation**, **pg2-nonprod EKS cost estimate**, **ATS/PDI monorepo Jenkins**. All items closed except **prod pgvalidate application logs** (intermittent Loki gaps from copytruncate + promtail glob). No backlog from 31 Aug handover.

---

## Closed

### ansible — Prod pgvalidate Loki (partial — see Open)
- Root cause identified: j21 writes `*service*.log`, promtail had `*application.log`; tomcat/access OK
- Git fix: j21 + j21-canary group_vars → `*service*.log`
- Loki connectivity verified; `timeout: 120s` applied on prod nodes
- Grafana query guidance: use `job`/`host` + tenant `ppsl-promotions-90d`, not alert labels

### pg2-nonprod — EKS migration cost estimate ✅
- 32 running nodes inventoried; ~$700/mo EC2 vs ~$570/mo EKS; Jira summary + Cost Explorer validation done

### merchant-deploy-k8s — ATS/PDI monorepo Jenkins ✅
- `Jenkinsfile-monorepo` + `Dockerfile-monorepo` for inventory-platform (ATS mfe1/mfe2 + PDI mfe)

### From 31 Aug handover — all resolved ✅
- Incentive J25 build/deploy
- Promtail AL2023 rollout (qa/staging promo boxes)
- CLM S3 AccessDenied
- TMS QA logs → Loki
- Prom sample_limit (dev-side metric reduction path)

---

## Open

| Item | State | Next step |
|------|-------|-----------|
| **Prod pgvalidate application logs → Loki** | Intermittent gaps on high-traffic j21 prod | Widen promtail glob (`*application.log*` or `*service*`), fix logrotate (`copy`+`create` not `copytruncate`), rollout j21 path fix to all prod nodes |

**Root cause (confirmed):** logrotate `maxsize 200M` + `copytruncate` rotates to timestamped files outside promtail glob → ~200–250MB lost per rotation (~every 4 min on busy nodes). Offset-ahead-of-file window also freezes reads briefly after each rotation.

---

## Context for next shift

### Prod pgvalidate app-log fix options
```yaml
# promtail — widen glob
__path__: /var/log/payment-promotion-validate/*application.log*
# or for j21: *service*.log / *{application,service}*
```
- Replace `copytruncate` with `copy` + `create` in logrotate (better long-term)
- Re-run `--tags promtail` on prod pgvalidate nodes after group_vars change

### Loki query (if debugging 5xx alerts)
```
{job="ppsl-promo-pgvalidate-tomcat-log", stack="prod", host="<IP>"} |= " 500 "
{job="ppsl-promo-pgvalidate-app-log", stack="prod", host="<IP>"}
```
Tenant: `ppsl-promotions-90d`

---

## Next steps (priority)

1. Fix prod pgvalidate app-log pipeline (glob + logrotate + rollout)
2. Verify app logs continuous in Loki after fix on busy prod node (e.g. 10.103.103.142)

---

## WhatsApp paste

*Day shift — 1 Sep*

*Closed:*
• pg2-nonprod EKS cost estimate ✅
• ATS/PDI monorepo Jenkins + Dockerfile-monorepo ✅
• 31 Aug backlog all done ✅ (Incentive, Promtail AL2023, CLM S3, TMS QA→Loki, Prom sample_limit)

*Open:*
• Prod pgvalidate **application logs → Loki** — intermittent gaps (copytruncate + narrow promtail glob, ~200MB lost per rotation on busy j21 nodes)

*Fix:*
• Widen glob *application.log* OR *service*.log | logrotate copy+create not copytruncate | rollout --tags promtail

*Night:*
1. Apply pgvalidate app-log fix on prod
2. Verify continuous Loki flow on high-traffic node

---

## Sources

- Chats: `a288f9e3`, `da4dd4a1`, `60bec1e4`
- Status confirmed by user 2026-09-01 19:31 IST
